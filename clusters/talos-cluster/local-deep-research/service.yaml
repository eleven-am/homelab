apiVersion: v1
kind: Service
metadata:
  name: local-deep-research
  namespace: local-deep-research
spec:
  selector:
    app: local-deep-research
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
