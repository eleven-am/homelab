apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-claim
  namespace: plex
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi-retain
  resources:
    requests:
      storage: 10Gi
