apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-claim
  namespace: plex
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: snow-storage
  resources:
    requests:
      storage: 10Gi
