apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: plex-route
  namespace: plex
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: gateway
  hostnames:
    - "plex.horus.ossai.dev"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: plex-service
          port: 32400
