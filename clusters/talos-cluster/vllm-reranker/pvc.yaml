apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-reranker-cache
  namespace: vllm-reranker
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: app-storage
  resources:
    requests:
      storage: 10Gi
