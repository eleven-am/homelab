apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: launchpad-data
  namespace: launchpad
spec:
  storageClassName: app-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
