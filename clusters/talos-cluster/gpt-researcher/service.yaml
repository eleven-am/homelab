apiVersion: v1
kind: Service
metadata:
  name: gpt-researcher-service
  namespace: gpt-researcher
spec:
  selector:
    app: gpt-researcher
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
