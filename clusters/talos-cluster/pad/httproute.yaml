apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pad-route
  namespace: pad
  labels:
    app: pad
spec:
  parentRefs:
    - name: gateway  # Replace with your actual Gateway name
      namespace: gateway-system  # Replace with your Gateway namespace
  hostnames:
    - "pad.maix.ovh"  # Replace with your actual domain
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: pad-service
          port: 80
          weight: 100
