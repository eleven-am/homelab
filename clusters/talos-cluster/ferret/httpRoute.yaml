apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ferret-route
  namespace: ferret
spec:
  parentRefs:
    - name: cloudflare-gateway
      namespace: cloudflare-gateway
  hostnames:
    - "ferret.maix.ovh"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ferret
          port: 80
