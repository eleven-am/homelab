apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ferret-data
  namespace: ferret
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: app-storage
  resources:
    requests:
      storage: 5Gi
