apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-api
  namespace: flow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flow-api
  template:
    metadata:
      labels:
        app: flow-api
    spec:
      imagePullSecrets:
        - name: zot-pull-secret
      initContainers:
        - name: init-db
          image: postgres:15-alpine
          env:
            - name: PGHOST
              value: "postgres-service.postgres.svc.cluster.local"
            - name: PGPORT
              value: "5432"
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
            - name: FLOW_DB_USER
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_USER
            - name: FLOW_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_PASSWORD
            - name: FLOW_DB_NAME
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_NAME
          command:
            - sh
            - -c
            - |
              echo "Checking if user $FLOW_DB_USER exists..."
              USER_EXISTS=$(psql -tAc "SELECT 1 FROM pg_roles WHERE rolname='$FLOW_DB_USER'")
              if [ "$USER_EXISTS" != "1" ]; then
                echo "Creating user $FLOW_DB_USER..."
                psql -c "CREATE USER $FLOW_DB_USER WITH PASSWORD '$FLOW_DB_PASSWORD' CREATEDB;"
              else
                echo "User $FLOW_DB_USER already exists, updating password..."
                psql -c "ALTER USER $FLOW_DB_USER WITH PASSWORD '$FLOW_DB_PASSWORD' CREATEDB;"
              fi

              echo "Checking if database $FLOW_DB_NAME exists..."
              DB_EXISTS=$(psql -tAc "SELECT 1 FROM pg_database WHERE datname='$FLOW_DB_NAME'")
              if [ "$DB_EXISTS" != "1" ]; then
                echo "Creating database $FLOW_DB_NAME..."
                psql -c "CREATE DATABASE $FLOW_DB_NAME OWNER $FLOW_DB_USER;"
              else
                echo "Database $FLOW_DB_NAME already exists"
              fi

              echo "Granting privileges..."
              psql -c "GRANT ALL PRIVILEGES ON DATABASE $FLOW_DB_NAME TO $FLOW_DB_USER;"
              echo "Database initialization complete!"
      containers:
        - name: flow-api
          image: zot.maix.ovh/flow-server:v0.2.7
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ENV
              value: "production"
            - name: PORT
              value: "8080"
            - name: LOG_LEVEL
              value: "info"
            - name: AUTO_MIGRATE
              value: "true"
            - name: ALLOWED_ORIGINS
              value: "https://flow.maix.ovh"
            - name: DB_HOST
              value: "postgres-service.postgres.svc.cluster.local"
            - name: DB_PORT
              value: "5432"
            - name: DB_SSL_MODE
              value: "disable"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_NAME
            - name: REDIS_URL
              value: "redis://redis-service.redis.svc.cluster.local:6379/1"
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: ENCRYPTION_KEY
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: SECRET_KEY
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: RABBITMQ_URL
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-core
  namespace: flow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flow-core
  template:
    metadata:
      labels:
        app: flow-core
    spec:
      imagePullSecrets:
        - name: zot-pull-secret
      containers:
        - name: flow-core
          image: zot.maix.ovh/flow-core:v0.2.7
          env:
            - name: ENV
              value: "production"
            - name: LOG_LEVEL
              value: "info"
            - name: DB_HOST
              value: "postgres-service.postgres.svc.cluster.local"
            - name: DB_PORT
              value: "5432"
            - name: DB_SSL_MODE
              value: "disable"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_NAME
            - name: REDIS_URL
              value: "redis://redis-service.redis.svc.cluster.local:6379/1"
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: ENCRYPTION_KEY
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: SECRET_KEY
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: RABBITMQ_URL
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-streaming
  namespace: flow
spec:
  replicas: 0
  selector:
    matchLabels:
      app: flow-streaming
  template:
    metadata:
      labels:
        app: flow-streaming
    spec:
      imagePullSecrets:
        - name: zot-pull-secret
      containers:
        - name: flow-streaming
          image: zot.maix.ovh/flow-streaming:v0.2.7
          env:
            - name: ENV
              value: "production"
            - name: LOG_LEVEL
              value: "info"
            - name: DB_HOST
              value: "postgres-service.postgres.svc.cluster.local"
            - name: DB_PORT
              value: "5432"
            - name: DB_SSL_MODE
              value: "disable"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_NAME
            - name: REDIS_URL
              value: "redis://redis-service.redis.svc.cluster.local:6379/1"
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: ENCRYPTION_KEY
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: SECRET_KEY
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: RABBITMQ_URL
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-stage-worker
  namespace: flow
spec:
  replicas: 0
  selector:
    matchLabels:
      app: flow-stage-worker
  template:
    metadata:
      labels:
        app: flow-stage-worker
    spec:
      imagePullSecrets:
        - name: zot-pull-secret
      containers:
        - name: flow-stage-worker
          image: zot.maix.ovh/flow-stage-worker:v0.2.7
          env:
            - name: ENV
              value: "production"
            - name: LOG_LEVEL
              value: "info"
            - name: DB_HOST
              value: "postgres-service.postgres.svc.cluster.local"
            - name: DB_PORT
              value: "5432"
            - name: DB_SSL_MODE
              value: "disable"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: DB_NAME
            - name: REDIS_URL
              value: "redis://redis-service.redis.svc.cluster.local:6379/1"
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: ENCRYPTION_KEY
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: SECRET_KEY
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: flow-secret
                  key: RABBITMQ_URL
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
