apiVersion: v1
kind: Service
metadata:
  name: convex-dashboard
  namespace: convex
spec:
  selector:
    app: convex-dashboard
  ports:
    - port: 6791
      targetPort: 6791
      name: http
  type: ClusterIP
